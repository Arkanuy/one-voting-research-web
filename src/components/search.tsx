"use client";
import {Search,X} from "lucide-react";import {useRouter} from "next/navigation";import {useEffect,useMemo,useRef,useState} from "react";import {searchDocuments,type SearchDocument} from "@/lib/search";
export function SearchBox({items}:{items:SearchDocument[]}){const[q,setQ]=useState(""),[active,setActive]=useState(0);const input=useRef<HTMLInputElement>(null);const router=useRouter();const hits=useMemo(()=>searchDocuments(items,q),[items,q]);useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();input.current?.focus()}if(e.key==="Escape"){setQ("");input.current?.blur()}};addEventListener("keydown",f);return()=>removeEventListener("keydown",f)},[]);function go(i:number){const h=hits[i];if(!h)return;setQ("");router.push(`/documents/${h.slug}#${h.anchor}`);requestAnimationFrame(()=>document.getElementById(h.anchor)?.scrollIntoView())}function key(e:React.KeyboardEvent){if(e.key==="ArrowDown"){e.preventDefault();setActive(a=>Math.min(a+1,hits.length-1))}if(e.key==="ArrowUp"){e.preventDefault();setActive(a=>Math.max(a-1,0))}if(e.key==="Enter"&&hits.length){e.preventDefault();go(active)}}return <div className="search-wrap"><Search/><input ref={input} id="research-search" value={q} onChange={e=>{setQ(e.target.value);setActive(0)}} onKeyDown={key} placeholder="Cari seluruh riset…" aria-label="Cari seluruh riset" role="combobox" aria-expanded={q.length>=2} aria-controls="research-search-results"/><kbd>Ctrl K</kbd>{q&&<button aria-label="Bersihkan pencarian" onClick={()=>setQ("")}><X/></button>}{q.length>=2&&<div id="research-search-results" className="search-results" role="listbox">{hits.length?hits.map((h,i)=><button role="option" aria-selected={i===active} className={i===active?"active":""} key={h.key} onMouseEnter={()=>setActive(i)} onClick={()=>go(i)}><strong>{h.section}</strong><em>{h.title}</em><span>…{h.snippet}…</span></button>):<p>Tidak ada hasil untuk “{q}”.</p>}</div>}</div>}
