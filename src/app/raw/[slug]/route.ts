import {getMarkdown,isDocumentSlug} from "@/lib/documents";export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!isDocumentSlug(slug))return new Response("Not found",{status:404});return new Response(getMarkdown(slug),{headers:{"Content-Type":"text/markdown; charset=utf-8","Content-Disposition":`attachment; filename="${slug}.md"`}})}
